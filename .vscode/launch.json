{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug with LLDB",
            "type": "lldb",
            "request": "launch",
            "windows": {"program":"${workspaceFolder}/build/${workspaceFolderBasename}.exe"},
            "linux": {"program":"${workspaceFolder}/build/${workspaceFolderBasename}"},
            "args": [],
            "cwd": "${workspaceFolder}",
            "terminal": "integrated",
            "stopOnEntry": false,
            "preLaunchTask": "CMake: build"
        }
    ]
}